# 万维文 WitWork · 使用说明

> 万维文 AI 写作：汇万千思路，写一纸文章。

本地优先的「人机实时协作写作系统」——AI 长篇写作 + 自有世界观知识库 + 一致性管理，全部在本地运行，离线可用、无云端。

---

## 一、启动

**方式一（推荐，双击即用）**：双击 `start.bat`
- 首次运行会自动建 Python 虚拟环境并安装依赖（需本机有 `python`）。
- 服务起来后自动打开浏览器到 `http://127.0.0.1:8723/`。
- 重复双击不会拉起第二份服务，只会再开一个浏览器标签。

**方式二（手动）**：在 `inkrealm/` 目录下
```
venv\Scripts\python.exe -m server.main
```
或（非 Windows）
```
PYTHONPATH=. venv/bin/python -m server.main
```
服务监听 `127.0.0.1:8723`（仅本机）。

> 浏览器关掉不影响写作，服务在后台继续跑。停止服务：关掉 `start.bat` 打开的那个黑色窗口（或结束 `python -m server.main` 进程）。

---

## 二、界面布局

```
┌ 顶栏：万维文 · 今日字数 · 保存状态 · [AI 助手] · 导出TXT · 设置 · 快照 ▸ ┐
├───────────┬───────────────────────────────┬──────────────────────────┤
│ 左栏标签   │          中栏：编辑器           │ 右栏标签                 │
│ 目录       │  章节标题                      │ 快照 / 人物 / 伏笔 /      │
│ 知识库     │  正文……                        │ 词条 / 爽点 / 统计 / 工具 │
│ AI 助手    │                                │                          │
│ 人格       │                                │                          │
│ 框架       │                                │                          │
└───────────┴───────────────────────────────┴──────────────────────────┘
```

- **左栏**：目录（作品/卷/章）、知识库、AI 会话、人格、框架共创。
- **中栏**：章节编辑（自动保存）、手机预览、一键排版。
- **右栏**：快照、人物、伏笔、词条、爽点节奏、写作统计、工具（导出/检查）。
- **设置**（顶栏「设置」按钮）：偏好 / 备份与恢复 / 导出 三页。
- **布局方案 A/B**（设置 → 偏好）：A 为上方三栏；B 把「AI 助手」独立成侧栏，由顶栏「AI 助手」按钮开关，可在左/右（设置里 `AI 栏位置`）。

---

## 三、开始写作

1. 左栏「目录」点 **＋作品**（或直接填作品名），会自动建「正文卷」和「第 1 章」并打开。
2. 在「目录」里继续加卷、加章；点章节即在中间编辑。
3. 输入 1.5 秒后自动保存；顶栏保存状态 `saved/saving/unsaved`。
4. 快捷键：`Ctrl+Z` 撤销、`Ctrl+Shift+Z` / `Ctrl+Y` 重做、`Ctrl+S` 立即保存、`Ctrl+F` 查找替换。
5. **快照**：右栏「快照」可手动存版本、对比差异、回滚（区别于自动保存，用于关键节点）。
6. **手机预览**：编辑器工具条「手机预览」看竖屏排版效果；「一键排版」按规范修正标点/引号/缩进（写回前自动建快照，可逐条接受/忽略）。

---

## 四、AI 协作

### 接入模型
- 默认已种两个 Provider：**本地演示 (mock)**（无需模型，离线假数据，用于体验界面）和 **本地 Ollama**（指向 `http://127.0.0.1:11434`）。
- 用真实模型前，先装并启动 [Ollama](https://ollama.com)，拉一个模型，例如：
  ```
  ollama pull qwen2.5:7b
  ollama pull qwen3:14b        # 更强
  ollama pull deepseek-r1:7b   # 推理型
  ```
- 左栏「AI 助手」里选 Provider 即可；点「测试」可探测连通性并列出本机模型。
- 想接其他 OpenAI 兼容服务：在「AI 助手」里新建 Provider，类型选 `openai`，填 `base_url` / `model` / `api_key`（key 存本地加密，不进云）。

### 怎么用
- 在输入框写指令（续写、润色、扩写、提问世界观……），回车或点发送，流式逐字输出，可随时「停止」。
- 发送时会**自动检索知识库**，把相关设定作为引用注入上下文，回复里带引用卡（点开看来源）。
- **写回提议**：AI 若产出应记进设定的内容，会以「写回提议」形式出现，你确认后才落库（不污染正文）。
- **人格**：默认「万维文」人格；可在「人格」页调整，或直接对 AI 说「记住：主角说话带江湖口吻」，会自动叠加为人格 overlay。
- **框架共创**：「框架」页用阶段机（简介→世界观→势力→情节线→人物→大纲→确认）逐步共建设定，末步可一键生成卷与章节骨架。

### 知识库（左栏「知识库」）
- 分区管理（世界观/人物/势力/情节/地点/词条/素材/其他）。
- 手动建条目，或导入 txt/md（按 `##` 切分条目）自动入库并嵌入。
- 检索 = 向量 + 关键词双路融合；嵌入默认 **哈希降级**（无需模型，离线可用）。

---

## 五、一致性工具（右栏 + 左栏）

- **人物**：人物卡与知识库双向同步；关系网；自动扫正文记录出场。
- **伏笔**：生命周期（规划→埋设→呼应→回收/废弃），每步留痕；定稿前提示未回收的高危伏笔。
- **词条**：新词发现（高频 n-gram）、异写归一化（自动把错写规范成标准词形并改写正文）。
- **角色模板**：5 个内置模板（主角/反派/配角/群像/组织），一键实例化。
- **爽点节奏**：7 类爽点标注 + SVG 曲线 + 偏离/平淡预警 + 插入建议。
- **搜索替换**：跨章全文搜索（正则/大小写/范围），可预览差异、全部替换、一键撤销。

---

## 六、统计与提醒

- 右栏「统计」：今日净增/新增/删除字数环图、连续天数、10 周热力图、目标设置。
- 断更提醒：连续 N 天（默认 3）没写，推送通知；可点「测试提醒」立即验证。

---

## 七、导出

- **Word**：右栏「工具」页 → 选字体/字号/行距/缩进 → 「导出 Word」。导出前经排版引擎，封面+目录+卷章正文，中文用你填的字体。
- **Markdown / EPUB / 设定 MD / 设定 JSON / TXT**：顶栏「设置」→「导出」页（需先打开一个作品）。设定导出包含知识库分区、人物、伏笔、关系。

---

## 八、备份与恢复（设置 → 备份与恢复）

- **一键备份**：把数据库 + 配置 + 上传打成 zip（默认排除密钥文件）。
- **恢复**：选一份备份「恢复」；**恢复前会自动再打一份 `pre-restore` 备份**，误操作可逆。
- **演练**：只解压校验完整性（不落盘），确认备份可恢复。
- **删除**：清理旧备份。
- **策略**：保留份数、每日自动备份（开服务期间每天一份）、是否排除密钥。

---

## 九、设置（顶栏「设置」）

- **主题**：暗色 / 亮色 / 护眼，即时生效。
- **编辑器**：字号、行距、页宽，即时生效。
- **布局**：方案 A/B；AI 栏位置 左/右。
- **默认 Provider / 嵌入模型 / 嵌入地址**：选默认 AI；嵌入默认哈希（免装），装了 `bge-m3` 可在设置里切到模型嵌入以获得更准检索。**切换嵌入模型后必须点一次「重建索引」（或调用 `POST /api/kb/reindex`），否则旧向量维度与新查询不匹配，检索会失效。**
- **自动排版 / 标点规范 / 自动写回 / 自动保存延迟 / 手机每页字数 / 断更提醒天数**。

---

## 十、故障排查

- **AI 一直 mock / 连不上**：确认 Ollama 已启动且模型已 `pull`；在「AI 助手」点「测试」看报错。Provider 的 `base_url` 默认 `http://127.0.0.1:11434`。
- **端口 8723 被占用**：改 `inkrealm/data/config.json` 里的 `port`（同时改 `host` 保持 `127.0.0.1`），重启服务。
- **检索不准 / 切换嵌入后检索失效**：默认哈希嵌入是离线兜底；装 `bge-m3` 后在设置切到模型嵌入，并务必点「重建索引」（或 `POST /api/kb/reindex`）对全库重嵌入，新查询才会用更优的 1024 维语义向量。Ollama 不可用时会自动回退哈希，检索仍可用。
- **数据在哪**：全部在 `inkrealm/data/`（数据库 `inkrealm.db`、配置 `config.json`、备份 `backups/`、导出 `exports/`）。定期用「备份与恢复」打包即可。

---

> 全部功能本地运行，不联网、不上云。AI 能力取决于你本机装的模型；写作与一致性管理不依赖任何外部服务。
