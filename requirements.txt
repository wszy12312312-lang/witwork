fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
python-multipart>=0.0.9
python-docx>=1.0
# 当前播放歌曲检测（Windows SMTC / 系统媒体传输控件）。仅 Windows 需要；缺失时自动降级为「手动填写」。
winsdk>=1.0.0b10; sys_platform == "win32"
# 音量控制（可选）：缺失时后端会自动 pip 安装；psutil 供 pycaw 识别会话进程。
pycaw>=20240210; sys_platform == "win32"
psutil>=5.9; sys_platform == "win32"
